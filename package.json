{
  "name": "pi-review",
  "version": "0.1.0",
  "description": "Adversarial multi-agent review pipeline for Pi",

  "scripts": {
    "build": "tsc --build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^20.19.41",
    "typescript": "^5.9.3",
    "vitest": "^1.6.1"
  },
  "type": "module",
  "pi": {
    "extensions": [
      "./src/extension/index.ts"
    ]
  }
}
